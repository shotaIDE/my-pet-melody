inputs:
  google-service-json-base64-dev:
    required: true

runs:
  using: "composite"
  steps:
    - name: Generate Google service plist file for dev
      shell: bash
      run: echo "${{ inputs.google-service-json-base64-dev }}" | base64 -d > android/app/firebase/dev/google-services.json
